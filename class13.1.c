#include <stdio.h>

int main(){
    /*
   //The multiples of 3
    int multiple;
    int prod;
    for(multiple = 1; multiple<= 5; multiple++)
    {
        prod = 3 * multiple;
        printf("The multiple of 3 and %d is %d\n", multiple, prod);
    }
    */
    //Thw multiples of 5 (<=20)
    int multiple;
    int prod;
    for(multiple = 1; multiple<=4; multiple++)
    {
        prod = 5 * multiple;
        printf("The multiple of 5 and %d is %d\n", multiple, prod);
    }
return 0;
}
